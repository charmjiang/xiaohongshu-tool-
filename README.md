# 小红书爆款文案生成器

## 启动步骤

1. 配置 API Key：
   ```bash
   cp .env.example .env
   # 编辑 .env，填入你的 ANTHROPIC_API_KEY
   ```

2. 启动：
   ```bash
   python3 app.py
   ```

3. 浏览器打开：http://localhost:5000

## 获取 API Key

去 https://console.anthropic.com 注册，充值后获取 API Key。
每篇文案费用约 $0.001（不到1分钱），非常便宜。
